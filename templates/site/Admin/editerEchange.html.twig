{% extends 'base.html.twig' %}

{% block title %}Trade{% endblock %}

{% block body %}

<div class="container">
    <h1>Editer un Echange</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success">Sauvegarder</button>
    {{form_end(form) }}
</div>

{% endblock %}